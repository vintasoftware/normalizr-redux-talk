import React from "react";

const Navbar = (props) => (
  <nav className="navbar navbar-dark bg-dark">
    <span className="navbar-brand mb-0 h1">normalizr-redux</span>
  </nav>
);

export default Navbar;